# LR0 Parser.

An implementation of LR0 Parser made in Python.

### How to run?

* Place grammar in `inputs` directory in the following format:
  ``` 
  START_SYMBOL
  RULE 1
  RULE 2
  .
  .
  RULE N
  ```
* Run the command `python lr0-parser.py <INPUT_FILENAME>` to generate the parsing table.
